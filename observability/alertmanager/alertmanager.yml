global:
  resolve_timeout: 5m

route:
  group_by: ["alertname", "severity"]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  receiver: default

receivers:
  - name: default
    # Configurer Slack/PagerDuty/email en production :
    # webhook_configs:
    #   - url: https://hooks.slack.com/services/XXX
    #     send_resolved: true
