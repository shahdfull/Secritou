# Matrice de couverture d'audit — Secritou
#
# Lignes : les 14 modules de REFERENTIEL.md §4 (+ une ligne "transverse" pour
# ce qui n'appartient à aucun module, cohérent avec anomalies/transverse.yaml).
# Colonnes : les 10 catégories d'audit A-J (voir liste ci-dessous).
# Chaque cellule : date ISO du dernier balayage ACTIF de cette catégorie sur
# ce périmètre, ou null si jamais balayé.
#
# Matrice remise à zéro le 2026-07-27 (décision explicite du porteur du
# projet) : on repart avec un système d'audit vierge, aucun périmètre n'est
# considéré comme déjà balayé dans ce nouveau système. L'historique réel
# (dates reconstituées depuis AUDIT_GRID.md / AUDIT_MATURITE_SECRITOU.md) est
# archivé sur la branche locale archive/anomalies-avant-reset (commit
# 3c03297), pas perdu — seulement retiré de ce fichier.
#
# Catégories A-J (poids, à confirmer/ajuster avec le porteur du projet si un
# futur document de maturité les redéfinit) :
#   A. Sécurité applicative — 18%
#   B. Intégrité des données — 15%
#   C. Performance et scalabilité — 10%
#   D. Fiabilité opérationnelle / DevOps — 10%
#   E. Couverture et qualité des tests — 10%
#   F. Conception API et cohérence des contrats — 6%
#   G. Exactitude métier et financière — 15%
#   H. UX/UI et accessibilité — 8%
#   I. Qualité et fiabilité de la documentation — 3%
#   J. Maturité du processus de release / CI-CD — 5%
#
# Règle de mise à jour (voir aussi CLAUDE.md) : avant de choisir un périmètre
# à auditer, lire cette matrice et prioriser la combinaison (perimetre,
# categorie) jamais balayée ou la plus ancienne — sauf signal plus fort
# (commit récent touchant ce périmètre, anomalie critique connue). Chaque
# audit doit mettre à jour la cellule (perimetre, categorie) réellement
# balayée avec la date du jour, à la fin de la session — jamais une autre
# cellule par supposition.

derniere_maj: "2026-07-30"
# 4.1-crm : A/B/G balayées le 2026-07-29 (audit réel contre REFERENTIEL.md
# §3.4/§4.1/§5 RG-002, lead.service.ts/.repository.ts/.controller.ts/
# .validator.ts, proposal.service.ts/.repository.ts/.controller.ts/
# .validator.ts, contact.service.ts, client/src/features/crm+leads+proposals) —
# SEC-018 (bloquant, régression validateur cassant tout le pipeline lead) et
# SEC-019 (bloquant, IDOR par arguments positionnels sur la création de
# proposition) trouvées et resolu (commit 1e27433, CI verte confirmée run
# 30465092019) — 4.1-crm remplit le critère de clôture "prêt au lancement"
# (CLAUDE.md). C/D/E/F/H/I/J laissées à null, non vérifiées cette passe.
# 4.4-facturation : A/B/G balayées le 2026-07-27 (premier audit réel du
# nouveau registre) — C/D/E/F/H/I/J laissées à null, non vérifiées cette
# passe.
# 4.6-portail-client : A/B balayées le 2026-07-27 — G non balayée (pas de
# calcul financier direct dans ce module) ; C/D/E/F/H/I/J laissées à null.
# 4.14-auth : A balayée le 2026-07-28 (vérification ciblée RBAC/rôles sur
# demande du porteur, pas un audit complet du module) ; B-J laissées à null.
# 4.5-commissions : A/B/G balayées le 2026-07-29 (audit réel contre
# REFERENTIEL.md §5, commission.service.ts/.controller.ts/.routes.ts/
# .validator.ts/.repository.ts + grep ciblé task.service.ts/schema.prisma) —
# SEC-015 (eleve) et SEC-016 (moyenne) trouvées, toutes deux resolu (commit
# fdc1363, CI verte confirmée run 30457571233) — aucune anomalie
# bloquant/eleve ouverte sur A/B/G : 4.5-commissions remplit le critère de
# clôture "prêt au lancement" (CLAUDE.md). C/D/E/F/H/I/J laissées à null,
# non vérifiées cette passe, n'entrent pas dans ce critère.
# 4.2-projets : A/B/G balayées le 2026-07-29 (premier audit réel du nouveau
# registre — project.service.ts/.repository.ts/.controller.ts,
# project.routes.ts, task.service.ts, timeEntry.service.ts/.repository.ts/
# .controller.ts, comment.service.ts/.controller.ts, document.service.ts,
# serviceScope.ts) — SEC-020 (bloquant, timeEntry.service.ts sans
# assertProjectInScope pour MANAGER) et SEC-021 (moyenne, création
# d'entrée de temps FREELANCER sans vérification que taskId lui est
# assigné) trouvées et resolu dans la même session (assertProjectInScope
# ajouté à create/list/mySummary, restriction assigneeId ajoutée pour
# FREELANCER dans create, nouveau server/test/timeEntryScope.test.ts, 7
# cas, appelle réellement timeEntryService) — aucune anomalie
# bloquant/eleve ouverte sur A/B/G : commit a8603d7, CI verte confirmée
# (6 checks GitHub Actions completed/success : client, server,
# contrast-check, i18n-check, restore-smoke-test, e2e) — 4.2-projets
# remplit le critère de clôture "prêt au lancement" (CLAUDE.md).
# C/D/E/F/H/I/J laissées à null, non vérifiées
# cette passe, n'entrent pas dans ce critère.
# 4.10-rbac : A/B balayées le 2026-07-29 (premier audit réel du nouveau
# registre — managerPermission.service.ts/.repository.ts/.controller.ts,
# permissionProfile.repository.ts/.controller.ts, managerPermission.routes.ts,
# permissionProfile.routes.ts, rbac.middleware.ts, managerPermission.validator.ts,
# client/src/types/permissions.ts) — surface d'administration RBAC
# elle-même saine (toutes les routes ADMIN-only, MODULES client/serveur
# synchronisés depuis SEC-025). SEC-022 (eleve, documentService.update/
# createVersion sans revérification du scope MANAGER malgré
# requirePermission("documents","update") en amont) découverte en
# vérifiant comment requirePermission est réellement consommé côté
# 4.2-projets — enregistrée et resolu sous 4.2-projets (pas ce périmètre,
# le défaut est dans le consommateur, pas dans 4.10 lui-même), commit
# b352fb5, CI verte confirmée (6 checks GitHub Actions completed/success :
# client, server, contrast-check, i18n-check, restore-smoke-test, e2e). G
# non balayée (pas de calcul financier direct dans ce module) ;
# C/D/E/F/H/I/J laissées à null.
# 4.13-notifications-jobs : A/B balayées le 2026-07-30 (premier audit réel
# du nouveau registre — jobs/queues.ts, jobs/index.ts,
# jobs/processors/communication.processor.ts,
# jobs/processors/documents.processor.ts,
# jobs/processors/maintenance.processor.ts (markOverdueInvoices,
# checkInvoiceFollowup via ceoAlerts.processor.ts), utils/webhook.ts,
# notification.service.ts/.repository.ts/.controller.ts,
# notification.routes.ts) — jobId déterministe correctement appliqué
# partout (notificationJobId/documentJobId/21 registrations cron avec
# jobId statique), aucun `:` dans un jobId, notification.repository.ts
# scope correctement markAsRead par (id, userId) combiné (IDOR
# impossible), webhook.ts signe en HMAC-SHA256 + timingSafeEqual +
# fenêtre de fraîcheur anti-rejeu. Fixes historiques (relances facture sur
# dueDate pas createdAt, relance après OVERDUE) confirmés toujours en
# place par lecture directe de checkInvoiceFollowup. Aucune anomalie
# nouvelle trouvée. G non balayée (pas de calcul financier propre à ce
# module — les jobs délèguent à invoiceService/commissionService déjà
# audités ailleurs) ; C/D/E/F/H/I/J laissées à null.
# transverse : A balayée le 2026-07-30 (premier audit réel du nouveau
# registre — RG-025 RGPD (gdpr.controller.ts/.service.ts/.routes.ts,
# self-service /me/export /me/erase confirmé scopé strictement sur
# req.user.sub), .github/workflows/ci.yml, .github/workflows/backup.yml,
# .github/dependabot.yml) — SEC-023 (moyenne, actions GitHub non
# épinglées par SHA malgré le TODO MED-1 documenté) et SEC-024 (moyenne,
# backup.yml SSH StrictHostKeyChecking=no) trouvées ; SEC-023 resolu
# (SHA épinglés sur les 3 actions utilisées, commit 0a26794, CI verte
# confirmée 9/9 checks) ; SEC-024 corrigée dans le code (SSH
# UserKnownHostsFile + secret SSH_KNOWN_HOSTS remplace
# StrictHostKeyChecking=no) mais `statut: ouvert` maintenu — non
# vérifiable de bout en bout avant l'existence d'un vrai serveur de
# production (SEC-217). Aucune des deux n'est bloquant/eleve, donc ne
# bloque pas la clôture "prêt au lancement" de ce périmètre au sens du
# critère CLAUDE.md. B/G non balayées (pas d'entité propre à ce
# périmètre) ; C/D/E/F/H/I/J laissées à null.
# 4.3-onboarding : A/B balayées le 2026-07-30 (premier audit réel du
# nouveau registre — clientOnboarding.service.ts/.repository.ts/
# .controller.ts, clientOnboarding.routes.ts) — SEC-025 (bloquant,
# updateOnboarding/updateStep/14 méthodes de sous-entité — dont Contract
# et Payment — sans vérification managerServiceId, contrairement aux 3
# méthodes de lecture déjà scopées) trouvée et resolu dans la même
# session (managerServiceId ajouté sur toute la chaîne repository/
# service/contrôleur, nouveau
# server/test/clientOnboardingWriteScopeManager.test.ts, 9 cas) — commit
# 61f6619, CI verte confirmée (6/6 checks GitHub Actions
# completed/success : server, client, restore-smoke-test,
# contrast-check, e2e, i18n-check) — 4.3-onboarding remplit le critère de
# clôture "prêt au lancement" (CLAUDE.md). G non balayée (Payment.amount
# est un champ saisi, pas un calcul propre à ce module) ; C/D/E/F/H/I/J
# laissées à null.
# 4.7-freelances : A/B balayées le 2026-07-30 (premier audit réel du
# nouveau registre — freelancer.service.ts/.repository.ts/.controller.ts,
# freelancer.routes.ts, freelancerApplication.service.ts/.routes.ts,
# rating.service.ts/.controller.ts/.routes.ts) — SEC-026 (eleve,
# freelancerService.getById sans scope MANAGER, contrairement à getAll)
# et SEC-027 (moyenne, ratingService.addRating/getRatingsByFreelancerId
# sans scope MANAGER) trouvées et resolu dans la même session (serviceId
# ajouté sur toute la chaîne repository/service/contrôleur pour les deux,
# nouveau server/test/freelancerRatingScopeManager.test.ts, 6 cas) —
# freelancerApplication.* confirmé ADMIN-only (aucune route MANAGER, pas
# de scope pôle applicable) — commit 93aaf71, CI verte confirmée (6/6
# checks GitHub Actions completed/success : server, client,
# restore-smoke-test, contrast-check, e2e, i18n-check) — 4.7-freelances
# remplit le critère de clôture "prêt au lancement" (CLAUDE.md). G non
# balayée (pas de calcul financier propre à ce module — hourlyRate est un
# champ saisi) ; C/D/E/F/H/I/J laissées à null.
# 4.8-analytics : A/B balayées le 2026-07-30 (premier audit réel du
# nouveau registre — gscConnection.service.ts/.controller.ts/.routes.ts,
# metricSnapshot.controller.ts, executiveMetrics.service.ts/.controller.ts,
# revenueForecast.service.ts/.controller.ts) — SEC-028 (eleve,
# gscConnectionService et getClientMetrics sans scope MANAGER, alors que
# executiveMetrics/revenueForecast étaient déjà correctement scopés)
# trouvée et resolu dans la même session (nouvelle assertClientInScope
# dans serviceScope.ts, appliquée aux 4 méthodes de gscConnectionService +
# getClientMetrics, nouveau
# server/test/gscConnectionScopeManager.test.ts, 6 cas) — commit
# 328a5a8, CI verte confirmée (6/6 checks GitHub Actions
# completed/success : server, client, restore-smoke-test,
# contrast-check, e2e, i18n-check) — 4.8-analytics remplit le critère de
# clôture "prêt au lancement" (CLAUDE.md). G non balayée (pas de calcul
# financier propre à ce module) ; C/D/E/F/H/I/J laissées à null.
# 4.12-site-public : A/B balayées le 2026-07-30 (premier audit réel du
# nouveau registre — siteContent.service.ts/.repository.ts/.controller.ts/
# .routes.ts/.validator.ts) — pas de notion de pôle applicable (contenu
# éditorial du site vitrine, company-wide par conception) ; écriture
# ADMIN-only confirmée (authorize("ADMIN") sur GET/PUT admin, lecture
# publique correctement limitée à key→value sans fuite de label/section/
# type internes). Aucune anomalie trouvée. G non balayée (pas de calcul
# financier) ; C/D/E/F/H/I/J laissées à null.

modules:
  "4.1-crm":
    label: "4.1 CRM & Pipeline commercial"
    A: "2026-07-29"
    B: "2026-07-29"
    C: null
    D: null
    E: null
    F: null
    G: "2026-07-29"
    H: null
    I: null
    J: null

  "4.2-projets":
    label: "4.2 Gestion de projet"
    A: "2026-07-29"
    B: "2026-07-29"
    C: null
    D: null
    E: null
    F: null
    G: "2026-07-29"
    H: null
    I: null
    J: null

  "4.3-onboarding":
    label: "4.3 Onboarding client"
    A: "2026-07-30"
    B: "2026-07-30"
    C: null
    D: null
    E: null
    F: null
    G: null
    H: null
    I: null
    J: null

  "4.4-facturation":
    label: "4.4 Facturation & Paiements"
    A: "2026-07-27"
    B: "2026-07-27"
    C: null
    D: null
    E: null
    F: null
    G: "2026-07-27"
    H: null
    I: null
    J: null

  "4.5-commissions":
    label: "4.5 Rémunération des associés (Commissions)"
    A: "2026-07-29"
    B: "2026-07-29"
    C: null
    D: null
    E: null
    F: null
    G: "2026-07-29"
    H: null
    I: null
    J: null

  "4.6-portail-client":
    label: "4.6 Portail client"
    A: "2026-07-27"
    B: "2026-07-27"
    C: null
    D: null
    E: null
    F: null
    G: null
    H: null
    I: null
    J: null

  "4.7-freelances":
    label: "4.7 Freelances"
    A: "2026-07-30"
    B: "2026-07-30"
    C: null
    D: null
    E: null
    F: null
    G: null
    H: null
    I: null
    J: null

  "4.8-analytics":
    label: "4.8 Analytics & Performance"
    A: "2026-07-30"
    B: "2026-07-30"
    C: null
    D: null
    E: null
    F: null
    G: null
    H: null
    I: null
    J: null

  "4.9-client-success":
    label: "4.9 Client Success"
    note: "Module GELÉ — voir CLAUDE.md « Statut GELÉ » : exclu de tout audit/développement sauf blocage du chemin de l'argent remonté ailleurs."
    A: null
    B: null
    C: null
    D: null
    E: null
    F: null
    G: null
    H: null
    I: null
    J: null

  "4.10-rbac":
    label: "4.10 RBAC & Permissions granulaires"
    A: "2026-07-29"
    B: "2026-07-29"
    C: null
    D: null
    E: null
    F: null
    G: null
    H: null
    I: null
    J: null

  "4.11-ia":
    label: "4.11 Module IA (agent-service)"
    note: "Module GELÉ — voir CLAUDE.md « Statut GELÉ »."
    A: null
    B: null
    C: null
    D: null
    E: null
    F: null
    G: null
    H: null
    I: null
    J: null

  "4.12-site-public":
    label: "4.12 Contenu du site public"
    A: "2026-07-30"
    B: "2026-07-30"
    C: null
    D: null
    E: null
    F: null
    G: null
    H: null
    I: null
    J: null

  "4.13-notifications-jobs":
    label: "4.13 Notifications & Automatisations"
    A: "2026-07-30"
    B: "2026-07-30"
    C: null
    D: null
    E: null
    F: null
    G: null
    H: null
    I: null
    J: null

  "4.14-auth":
    label: "4.14 Authentification & Compte utilisateur"
    A: "2026-07-28"
    B: null
    C: null
    D: null
    E: null
    F: null
    G: null
    H: null
    I: null
    J: null

  "transverse":
    label: "Transverse (hors modules §4 — CI/infra, RGPD, docs, i18n global, design system, observabilité)"
    A: "2026-07-30"
    B: null
    C: null
    D: null
    E: null
    F: null
    G: null
    H: null
    I: null
    J: null
