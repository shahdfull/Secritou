-- Rename relation label from "LeadConvertedClient" to "ConvertedLead" (Prisma-level only).
-- The FK constraint already exists from migration 20260622194500; no DB change needed.
-- This migration is a no-op at the SQL level; it exists to keep the migration history aligned
-- with the schema after renaming the @relation name in schema.prisma.
SELECT 1;
