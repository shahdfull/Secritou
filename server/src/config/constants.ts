/**
 * Application-wide constants.
 *
 * COMPANY_ID is the stable UUID for the single "Secritou" company record.
 * It is also used in prisma/seed.ts so the two are always in sync.
 */
export const COMPANY_ID = "00000000-0000-0000-0000-000000000001";
