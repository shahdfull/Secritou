# Audit Round 4 — Sécurité & intégrité des données — Secritou

> Contexte : ce rapport reconstruit intégralement, à partir du code source actuel, les constats de sécurité que les rounds 1-3 auraient produits. **Les documents de ces rounds précédents sont introuvables** (absents du dépôt, de l'historique git, de tout fichier mémoire) — ils sont considérés comme irrécupérables. Aucune affirmation de ce rapport ne s'appuie sur un souvenir ou une hypothèse de correctif antérieur : chaque verdict ci-dessous découle d'une lecture directe du code tel qu'il existe aujourd'hui (2026-07-11), avec raisonnement explicite sur le scénario d'exploitation quand c'est pertinent.
>
> Deux documents d'audit existants dans le dépôt (`audit/10-audit-metier.md`, `WORKFLOW_AUDIT_REPORT.md`) couvrent la logique métier/workflow, pas la sécurité — ils ont été utilisés uniquement comme pointeurs vers des fichiers/lignes, sans reprendre leurs verdicts "OK" tels quels. Plusieurs constats ci-dessous **contredisent** des affirmations de `10-audit-metier.md` (ex. : routes analytics prétendues `authorize("ADMIN")` seul — en réalité `authorize("ADMIN", "MANAGER")` avec scope aujourd'hui), signe que le code a bougé depuis cette lecture partielle.

---

## Rapport 1 — Points nommés du round 4 (ré-dérivés indépendamment)

| Point vérifié | Fichier + ligne | Constat | Statut |
|---|---|---|---|
| **1. `addPayment` sur facture CANCELLED** | `server/src/services/invoice.service.ts:195-201` | `addPayment` charge l'invoice dans la transaction et vérifie `if (!["SENT", "PARTIAL", "OVERDUE"].includes(invoice.status)) throw new HttpError(409, ...)` — `CANCELLED`, `DRAFT` et `PAID` sont donc explicitement exclus. Un paiement ne peut pas être enregistré sur une facture annulée. | ✅ Appears fine |
| **2. Fuite `hourlyRate`** | `server/src/controllers/freelancer.controller.ts:6-42` | `redactSensitiveInfo()` retire `hourlyRate`/`email`/`bio` uniquement quand `callerRole === "FREELANCER"` et que ce n'est pas son propre profil. La route `GET /freelancers` et `GET /freelancers/:id` (`server/src/routes/freelancer.routes.ts:13,23`) n'autorise que `ADMIN`, `MANAGER`, `FREELANCER` — **CLIENT n'a jamais accès à cette route**, donc pas de fuite vers CLIENT par ce chemin. MANAGER/ADMIN voient le taux en clair (attendu — ce sont eux qui gèrent la facturation). FREELANCER→FREELANCER est bien redacté. Aucun vecteur de fuite trouvé vers un rôle qui ne devrait pas voir le taux. | ✅ Appears fine (sur le périmètre vérifiable) |
| **3. Cache cross-scope analytics/dashboard** | `server/src/services/executiveMetrics.service.ts:9-21` (clé `executive:metrics:v1[:serviceId]`), `server/src/routes/analytics.routes.ts:17-38`, `server/src/controllers/dashboard.controller.ts:5-23`, `server/src/services/dashboard.service.ts:43-56` | La majorité des endpoints analytics (`/analytics/executive`, `/analytics/revenue-forecast`, `/analytics/client-profitability`, `/analytics/summary`, `/dashboard/summary`, `/dashboard/full`) dérivent bien un `serviceId` scope via `buildServiceScope(req)` pour MANAGER et l'incluent dans la clé de cache. **Mais** `GET /summary/dashboard` (`server/src/routes/summary.routes.ts:15`, `server/src/controllers/summary.controller.ts:32-39`, `server/src/services/summary.service.ts:33-45`) autorise `ADMIN` **et** `MANAGER`, mais `getEnhancedDashboardSummary()` ne prend **aucun paramètre de scope**, utilise l'unique clé globale `cache:dashboard:summary:${COMPANY_ID}` (`cacheKeys.ts:4`), et la requête sous-jacente (`summary.repository.ts:103-142`) n'a **aucun filtre `serviceId`** sur les agrégats (`lead.groupBy`, `client.count`, `project.groupBy`, `task.groupBy`, `invoice.aggregate`). Résultat : un MANAGER de pôle A appelant `/summary/dashboard` reçoit les totaux **de toute l'agence, tous pôles confondus**, et cette réponse est partagée (même clé de cache) entre tous les appelants ADMIN/MANAGER. C'est une violation directe du modèle de scope appliqué partout ailleurs (`dashboard.service.ts`, `executiveMetrics.service.ts`, `analytics.routes.ts`). | ❌ Toujours cassé — nouvelle découverte (voir Synthèse, sévérité Élevée) |
| **4. Clé HMAC/chiffrement GSC** | `server/src/utils/encryption.ts:12-22`, `server/src/services/gscConnection.service.ts:23-30` | AES-256-GCM avec IV aléatoire par message, `authTag` vérifié au déchiffrement — implémentation correcte, pas de clé en dur. `INTEGRATIONS_ENCRYPTION_KEY` est réutilisée comme secret HMAC pour signer le `state` OAuth (commentaire explicite ligne 24-25 justifiant la réutilisation, TTL 10 min sur le state, ligne 21). Seul bémol : `env.ts:67` déclare `INTEGRATIONS_ENCRYPTION_KEY: z.string().optional()` — la clé n'est **pas validée au démarrage du serveur**, l'échec (`HttpError 500`) ne survient qu'au premier appel réel (connexion GSC). Risque opérationnel (déploiement sans la clé passe le boot puis casse en prod à la première utilisation), pas une brèche de sécurité en soi. | ⚠️ Partiel — implémentation crypto correcte, validation d'env différée à l'exécution |
| **5. Scope MANAGER sur `Approval`** | `server/src/services/approval.service.ts:15-27` (`assertApprovalInScope`) | Pattern identique à `assertProjectInScope`/`assertProposalInScope` : throw 404 (pas 403) si l'approval a un projet hors du service du MANAGER, no-op si `approval.projectId` est null (service-neutre) ou si le rôle n'est pas MANAGER. Appelé de façon cohérente dans `getById`, `update`, `delete`, `approve`, `reject`, `comment`, `addAttachment`, `deleteAttachment`. Routes (`server/src/routes/approval.routes.ts:40-53`) : toutes `authorize("ADMIN","MANAGER")` + `requirePermission("approvals", ...)`. | ✅ Appears fine |
| **6. Déduplication de paiement** | `server/src/services/invoice.service.ts:198-216` | Double garde : (a) si `idempotencyKey` fourni, recherche exacte `payment.findUnique({ where: { idempotencyKey } })` et retourne le paiement existant si déjà présent pour cette facture (ligne 204-208) ; (b) sinon fallback sur une fenêtre de 10 secondes (même invoice, même montant, même `recordedById`, ligne 210-213). Le tout à l'intérieur d'un seul `prisma.$transaction`, donc pas de race entre la vérification et l'écriture pour deux requêtes séquentielles. **Point d'attention réel** : le fallback sans `idempotencyKey` n'est protégé par aucune contrainte unique en base (contrairement au chemin `idempotencyKey`, qui repose sur une contrainte `@unique` Prisma) — deux requêtes strictement concurrentes (arrivant dans la même fenêtre de quelques millisecondes, avant que la première n'ait committé) pourraient in théorie passer la vérification `findFirst` avant que le `payment.create` de l'autre ne soit visible, si elles s'exécutent dans deux transactions Prisma séparées avec le niveau d'isolation par défaut (READ COMMITTED côté Postgres) — dans ce cas rien n'empêche un double `payment.create` pour le même montant sans idempotencyKey. Sévérité limitée : nécessite un vrai double-clic quasi simultané ET l'absence du header idempotency (le front devrait le fournir systématiquement — non vérifié ici, le front n'a pas été audité pour ce point). | ⚠️ Partiel — protection solide si `idempotencyKey` est utilisée systématiquement côté front (non vérifié), fenêtre de course théorique sur le fallback sans clé |
| **7. `applyCredit`** | `server/src/services/creditNote.service.ts:88-140` | Analyse ligne à ligne : (a) invoice `PAID`/`CANCELLED` rejetée avant toute écriture (lignes 96-97) ; (b) le credit note est marqué appliqué via un `update` conditionnel `where: { id, appliedAt: null, clientId: invoice.clientId }` (lignes 100-106) — Prisma lève une erreur si aucune ligne ne correspond (déjà appliqué OU client différent), ce qui **empêche structurellement la double-application et l'application à un client différent**, y compris sous concurrence (la contrainte est portée par la clause `WHERE` de l'`UPDATE`, atomique côté SQL) ; (c) montant appliqué plafonné par `Math.min(cn.amount, remaining, clientBalance)` (ligne 115) — ne peut jamais dépasser le solde restant de la facture ni le solde crédit du client ; (d) si `applicable <= 0`, rollback explicite du flag `appliedAt`/`appliedToInvoiceId` avant de lever une erreur (lignes 117-124), évitant de "griller" un avoir sans effet. | ✅ Appears fine — logique solide, aucun bug trouvé |

**C1-C7 / conformité produit (noms des pôles, `LegalPage.tsx`, devise, `conversionRate`) : non vérifiés.** Ces items dépendaient entièrement du round 3 manquant pour leur définition exacte (quels chemins d'exploitation "C1"-"C7" désignaient précisément) ; sans ce référentiel, toute tentative de les "vérifier" serait une reconstruction inventée plutôt qu'une vérification. Ils sont explicitement exclus de ce rapport plutôt que traités superficiellement.

---

## Rapport 2 — B1 à B6 (domaines nouveaux, aucune confiance dans une affirmation préalable)

| Constat | Fichier + ligne (preuve) | Sévérité | Comment vérifier/reproduire | Correctif proposé + effort |
|---|---|---|---|---|
| **B1a. Aggregates financiers/exécutifs ne filtrent jamais les clients/projets soft-deleted** | `server/src/repositories/executiveMetrics.repository.ts` (fichier entier, 526 lignes — aucune occurrence de `deletedAt` dans tout le fichier ; `clientProfitability.repository.ts:32-55` idem ; `revenueForecast.repository.ts` idem) | **Élevée** | Soft-supprimer un client avec des factures payées (`DELETE /clients/:id` → `deletedAt` posé, `client.repository.ts:105-107`), relancer `/analytics/executive` : `cashYTD`, `cashTotal`, `topClients`, `clientProfitability.totalRevenue` continuent d'inclure ses paiements/factures historiques. Idem pour un projet soft-supprimé (`project.repository.ts:142-143`) : `projectStatusCounts`, `total`, `completedDurations` le comptent toujours tant que `archivedAt` n'est pas aussi posé (seul `healthBoard.repository.ts:34` filtre `archivedAt: null` — pas `deletedAt`). | Ajouter `client: { deletedAt: null }` / `project: { deletedAt: null }` dans les scopes Prisma des 3 repositories (`executiveMetrics`, `clientProfitability`, `revenueForecast`). Effort faible à moyen (nombreux points d'injection dans `executiveMetrics.repository.ts`, mais mécanique). |
| **B1b. Invoice/Task listing n'excluent pas les parents (Client/Project) soft-deleted** | `server/src/repositories/invoice.repository.ts:24-50, 76-110` (filtre `deletedAt: null` sur `Invoice` seul, jamais sur `client`/`project`) ; `server/src/repositories/task.repository.ts:13-36` (`buildWhere` — aucun `deletedAt` du tout, ni sur `Task` ni sur `project`) | **Moyenne** | Soft-supprimer un `Project`, vérifier que ses `Task` restent listables via `GET /tasks?projectId=` (aucun filtre les exclut). Un client soft-supprimé garde ses factures visibles dans `/invoices` (ADMIN/MANAGER) tant que la facture elle-même n'est pas supprimée séparément. | Ajouter le filtre parent dans `taskRepository.buildWhere` et dans les `where` d'`invoiceRepository`. Effort faible. |
| **B1c. `restore` sur Client/Project ne restaure/re-vérifie jamais l'état des enfants** | `server/src/repositories/client.repository.ts:109-111`, `server/src/repositories/project.repository.ts:154-160` | **Faible** | `restore()` ne fait qu'un `update({ deletedAt: null })` sur la ligne elle-même. Les enfants (Task, Invoice, Document) n'ont pas leur propre `deletedAt` posé par la suppression du parent (ils ne sont jamais soft-supprimés en cascade) — donc pas de bug de "désynchronisation enfant non restauré" au sens strict, mais cela confirme B1a/B1b : la notion de "supprimé" n'est portée que par la ligne parente, jamais propagée aux requêtes de listing des enfants ni aux agrégats. | Documenté comme corollaire de B1a/B1b plutôt que bug indépendant. |
| **B1d. Suppression dure (hard DELETE) de factures/paiements** | `server/src/routes/invoice.routes.ts:73` (`DELETE /:id`) → `invoiceRepository.delete` (`invoice.repository.ts:145-147`) | ✅ Non trouvé | La route `DELETE /invoices/:id` existe mais `invoiceRepository.delete()` exécute `prisma.invoice.update({ data: { deletedAt: new Date() } })` — c'est un **soft delete**, pas un hard delete. Aucun `prisma.invoice.delete()`/`payment.delete()` trouvé dans tout `server/src` (grep ciblé). Le commentaire ligne 141-142 confirme l'intention ("Invoices are never hard-deleted: numbering must stay gapless"). | — |
| **B2. `$executeRawUnsafe` dans les règles d'archivage** | `server/src/jobs/processors/maintenance.processor.ts:81-85, 95-103` | **Faible (aujourd'hui) / risque latent** | `ensureMonthlyPartitions`/`archiveTableRows` interpolent `archiveTable`, `sourceTable`, `whereClause`, `rangeStart`, `next` directement dans du SQL brut via template string JS (pas de tagged template Prisma). Aujourd'hui ces valeurs viennent uniquement du tableau `ARCHIVE_RULES` codé en dur (pas d'entrée utilisateur) — non exploitable en l'état. Mais le pattern est fragile : toute évolution future ajoutant une règle paramétrable (ex. actuellement via config/API) serait immédiatement injectable, aucune validation/allowlist des noms de table n'existe en dehors du tableau constant. | Documenter/commenter clairement "ne jamais rendre `ARCHIVE_RULES` configurable sans allowlist stricte des noms de table" ; envisager de passer les bornes de dates (`rangeStart`, `next`) en paramètres liés plutôt qu'en interpolation de chaîne. Effort faible. |
| **B2b. Autres `$queryRaw`/`$executeRaw`** | `server/src/utils/projectProgress.ts:14-25` (tagged template avec `${projectIds}::text[]`), `server/src/routes/index.ts:105` (`SELECT 1` healthcheck, pas de variable) | ✅ Sûr | `projectProgress.ts` utilise le tagged template Prisma (`$queryRaw\`...${x}...\``), qui échappe automatiquement — pas de concaténation de chaîne. Pas de `search.service.ts` trouvé dans le dépôt (fichier non présent) ; les recherches `contains`/`insensitive` observées dans `invoice.repository.ts`, `client.repository.ts` etc. passent par l'opérateur Prisma paramétré, pas de risque d'injection ni de DoS regex (Postgres `ILIKE`, pas de moteur regex catastrophique). | — |
| **B3. Dashboard exécutif — écarts de réconciliation** | Voir B1a ci-dessus (racine du problème) + `executiveMetrics.repository.ts:394` (`allInvoices = [...(serviceId ? [] : c.invoices), ...]`) | **Élevée** (recoupe B1a) | Le calcul `cashMTD`/`cashYTD`/`topClients.revenue` intègre les paiements de clients/projets supprimés — un CEO regardant "cash encaissé ce mois" verrait un chiffre gonflé par des clients qui n'existent officiellement plus dans l'outil. Le reste de la logique (bornes de dates MTD/YTD via `startOf()`, distinction cash vs billed, fenêtres de forecast 30/60/90j) a été relu ligne à ligne et est cohérent — pas d'erreur d'inclusif/exclusif ni de double-comptage Invoice/Payment/CreditNote trouvée par ailleurs. | Corrélé au correctif B1a. |
| **B4a. PDF non régénéré après modification** | `server/src/services/documentGenerator.service.ts` (fichier entier, 541 lignes) — aucun mécanisme de re-génération automatique trouvé ; génération déclenchée uniquement à des points de cascade (`proposal.service.ts` à l'acceptation, `project.service.ts`) | **Moyenne** | Une facture d'acompte PDF (`generateInvoicePDF`) est générée une fois à l'acceptation de la proposition. Si le manager modifie ensuite les line items de la facture (autorisé tant qu'elle est `DRAFT`, `invoice.service.ts:120-131`), le PDF déjà généré et stocké (`Document.url`) reste l'ancienne version — rien ne le régénère ni ne le marque périmé. Un client pourrait recevoir/télécharger un PDF avec des montants différents de ceux affichés dans l'app. | Invalider/regénérer le PDF (ou au moins flag `stale: true` sur le Document) quand `invoice.service.ts:update`/`addItem`/`updateItem`/`deleteItem` modifie une facture qui a déjà un document généré. Effort moyen. |
| **B4b. Injection HTML/XSS dans le PDF** | `server/src/services/documentGenerator.service.ts:61-75` (`buildPdf`, moteur `pdfkit`) | ✅ Non applicable | Le générateur utilise **pdfkit** (dessin natif de PDF, `doc.text(...)`), pas un moteur HTML/headless browser (pas de Puppeteer/Playwright dans ce fichier). Le texte libre (nom client, description) est rendu comme texte brut dans le PDF — aucun risque d'exécution de script, l'unique risque théorique serait un débordement de mise en page avec une chaîne très longue (non testé, impact cosmétique seulement). | — |
| **B4c. URL de document signée à durée trop longue exposée directement au front** | `server/src/services/upload.service.ts:246-248` (`getSignedReadUrl(key, 60*60*24*7)`, 7 jours), `server/src/services/documentGenerator.service.ts:152-167` (`uploadAndCreate` stocke ce lien dans `Document.url`/`fileUrl`), `client/src/features/documents/DocumentsPage.tsx:240,243` (`window.open(doc.url, ...)`, `a.href = doc.url`) | **Élevée** | Le flux "propre" existe (`document.service.ts:94-100`, `getDownloadUrl` — re-signe une URL à TTL court 3600s **après** vérification du scope via `documentRepository.findById(id, viewer)`), mais le front n'est pas cohérent : le bouton voir/télécharger de `DocumentsPage.tsx` ouvre directement `doc.url`, c'est-à-dire l'URL signée longue durée (7 jours) déjà persistée en base et renvoyée telle quelle par `GET /documents`. Toute personne qui intercepte/partage cette URL (log navigateur, proxy, capture d'écran d'une barre d'adresse, exfiltration réseau) peut télécharger le PDF **sans passer par l'authentification applicative**, pendant 7 jours, indépendamment du rôle/scope du porteur du lien. | Ne jamais persister ni renvoyer d'URL signée longue durée dans la réponse de liste de documents ; stocker uniquement la `fileKey` côté API publique et forcer tout accès via `getDownloadUrl` (TTL court + contrôle de scope). Front : remplacer `doc.url` par un appel à l'endpoint de download signé à la demande. Effort moyen (touche front + back). |
| **B4d. `GET /upload/signed-url` sans contrôle de propriété de la clé** | `server/src/routes/upload.routes.ts:48`, `server/src/controllers/upload.controller.ts:81-100` | **Moyenne** | La route exige seulement `authenticate` (n'importe quel rôle authentifié, y compris CLIENT/FREELANCER) et valide juste que la clé ne contient pas `..` ni ne commence par `/` — **aucune vérification que l'appelant a le droit de voir ce fichier précis**. Si un CLIENT/FREELANCER connaît ou devine la clé S3 d'un document d'un autre client (clé = `<folder>/<uuid>.<ext>`, donc peu devinable en pratique car uuid v4, mais réutilisable si la clé fuite ailleurs, ex. via B4c), il peut se re-signer une URL fraîche lui-même via cet endpoint générique. | Restreindre cette route à des contextes internes (upload par l'app elle-même) ou faire porter la vérification de propriété sur le `key` (lookup `Document`/`FreelancerProfile` par `fileKey` + scope). Effort faible à moyen. |
| **B5a. Module de permission fantôme `freelancers`** | `server/src/services/managerPermission.service.ts:7-19` (`MODULES` inclut `"freelancers"`), grep exhaustif de `requirePermission("freelancers"` sur tout `server/src/routes` : **0 résultat** ; `server/src/routes/freelancer.routes.ts` (fichier entier) n'utilise que `authorize(...)`, jamais `requirePermission` | **Moyenne** | Un ADMIN configurant, via l'écran de permissions granulaires, `freelancers.read = false` pour un MANAGER donné n'a **aucun effet réel** : la route `GET /freelancers` ne vérifie que `authorize("ADMIN","MANAGER","FREELANCER")`, jamais `requirePermission("freelancers", ...)`. Le module existe dans le schéma de permissions (donc visible/configurable dans l'UI des paramètres) mais ne protège rien — un faux sentiment de contrôle d'accès. | Ajouter `requirePermission("freelancers", "read"/"update")` sur les routes `freelancer.routes.ts` pertinentes pour MANAGER, cohérent avec les 10 autres modules qui sont tous effectivement appliqués (vérifié : `projects`, `tasks`, `clients`, `leads`, `invoices`, `analytics`, `approvals`, `documents`, `proposals`, `service-requests` ont tous au moins un `requirePermission` correspondant dans les routes). Effort faible. |
| **B5b. Ordre scope vs permission** | `server/src/services/invoice.service.ts:29-42` (`assertInvoiceInScope` renvoie 404), `server/src/middlewares/rbac.middleware.ts:22-45` (`requirePermission` renvoie 403) | ✅ Cohérent | `requirePermission` s'exécute **avant** d'atteindre le service (middleware de route), donc un MANAGER sans la permission `invoices.read` reçoit 403 avant même que le scope ne soit évalué — pas de fuite d'information différentielle observée entre "403 permission manquante" et "404 hors scope" dans ce sens (permission d'abord, scope ensuite). Dans l'autre sens (permission OK, scope KO), le 404 est intentionnellement choisi (commentaire explicite ligne 28 d'`invoice.service.ts` et ligne 14 d'`approval.service.ts` : "to avoid leaking existence of out-of-scope"). Cohérent et documenté. | — |
| **B6a. Templates email HTML sans échappement** | `server/src/services/emailTemplates/base.ts:72-98` (`h1`, `p`, `infoBox` — interpolation directe `${text}`/`${value}` sans échappement HTML) | **Faible à moyenne** | Un nom de client, une note de lead, ou un commentaire d'approbation contenant `<img src=x onerror=alert(1)>` ou un lien `<a href="http://phishing...">` serait injecté tel quel dans l'email HTML envoyé (ex. `approvalRequestedTemplate`, `invoiceSentTemplate` interpolent des champs utilisateur via ces helpers). La plupart des clients mail modernes bloquent le JS et le CSS externe, donc l'exécution de script est peu probable ; le risque réaliste est une **casse de mise en page** ou une **injection de lien trompeur** dans un email qui semble provenir de l'agence. | Échapper les entités HTML (`&`, `<`, `>`, `"`, `'`) dans `p()`/`h1()`/`infoBox()` avant interpolation, ou passer par un template engine qui échappe par défaut. Effort faible. |
| **B6b. Cohérence domaine expéditeur** | `server/src/services/email.service.ts:56` (`"Secritou" <noreply@secritou.com>`), `server/src/config/env.ts:21-22` (`hello@secritou.com`) | **Info** | Domaine `.com` partout dans le code, alors que le contexte produit mentionne `.tn` (Tunisie) — probablement un domaine par défaut/placeholder plutôt qu'un bug de sécurité ; à confirmer avec l'équipe si le domaine de prod réel diffère (SPF/DKIM/DMARC à vérifier séparément, hors du code source). | Aligner avec le domaine réellement possédé et configurer SPF/DKIM/DMARC (hors périmètre code). |
| **B6c. Liens magiques / expiration** | `server/src/services/auth.service.ts:153-190` (reset password : token aléatoire 32 bytes, hashé en base, expire après 1h, vérifié via `resetTokenExpiry: { gt: new Date() }`) | ✅ Appears fine | Pas de "lien magique" permanent trouvé pour le portail client — l'authentification du portail client passe par login classique (JWT), pas par lien email à usage permanent. Le flux reset password est correctement implémenté (token hashé, TTL 1h, usage unique via mise à null après consommation ligne 190). | — |

---

## Synthèse consolidée — nouvelle source de vérité (findings vérifiés dans cette session uniquement)

> Rappel : les items round 1-3 (addPayment/cancelled, hourlyRate leak, cache cross-scope générique, HMAC GSC, scope MANAGER Approval, dédup paiement, applyCredit) ne sont **pas reportés ici comme "non résolus historiques"** — seul ce qui a été personnellement vérifié dans cette session apparaît. Le Rapport 1 ci-dessus fait foi pour ces points ; 5 des 7 sont ✅, 2 sont ⚠️ (aucun n'est ❌ franc).

### Sévérité Élevée
1. **`GET /summary/dashboard` fuit des données tous-pôles à un MANAGER scopé** — `server/src/services/summary.service.ts:33-45`, `server/src/repositories/summary.repository.ts:103-142`. Seul endpoint dashboard qui casse le modèle de scope par ailleurs cohérent partout dans le code.
2. **Agrégats financiers exécutifs (cash, forecast, profitabilité client) n'excluent jamais les clients/projets soft-supprimés** — `executiveMetrics.repository.ts`, `clientProfitability.repository.ts`, `revenueForecast.repository.ts` (aucun filtre `deletedAt`). Impact direct sur la fiabilité des chiffres présentés au CEO (B1a/B3).
3. **URL de document signée 7 jours persistée et exposée directement par l'API de liste, contournée par le front au lieu du flux de download scopé/court** — `upload.service.ts:246-248`, `documentGenerator.service.ts:159-160`, `DocumentsPage.tsx:240,243`. Fuite d'accès non authentifié à des documents potentiellement sensibles (contrats, factures) si l'URL est interceptée/partagée.

### Sévérité Moyenne
4. Task/Invoice listing n'excluent pas les enfants dont le parent (Project/Client) est soft-supprimé (B1b).
5. PDF de facture/devis non régénéré après modification des line items — risque de document envoyé avec des montants obsolètes (B4a).
6. `GET /upload/signed-url` permet à tout utilisateur authentifié de re-signer une URL pour n'importe quelle clé S3 sans vérification de propriété (B4d).
7. Module de permission `freelancers` déclaré mais jamais appliqué (phantom permission) — faux sentiment de contrôle d'accès pour l'ADMIN qui le configure (B5a).
8. Templates email HTML sans échappement des variables utilisateur — risque de casse de mise en page / lien trompeur (B6a).

### Sévérité Faible / latente
9. `INTEGRATIONS_ENCRYPTION_KEY` optionnelle dans le schéma d'environnement — échec différé à l'exécution plutôt qu'au démarrage (Rapport 1, point 4).
10. Fenêtre de course théorique sur la déduplication de paiement sans `idempotencyKey` (Rapport 1, point 6).
11. `$executeRawUnsafe` dans `maintenance.processor.ts` — non exploitable aujourd'hui (entrées codées en dur) mais pattern fragile pour une évolution future (B2).

---

## Zones d'ombre

- **Pas de base de données de test/dev interrogeable** dans cet environnement — le point B3 (réconciliation des métriques dashboard) a été traité par **relecture manuelle intégrale** de `executiveMetrics.repository.ts` (526 lignes), `revenueForecast.repository.ts` (141 lignes), `clientProfitability.repository.ts` (132 lignes) et `healthBoard.repository.ts` (129 lignes), pas par exécution de SQL comparatif réel. Les écarts trouvés (B1a) sont des constats de structure de requête (absence de filtre), pas des diffs numériques mesurés sur des données réelles.
- **Fenêtre de course sur `addPayment` sans `idempotencyKey`** (Rapport 1, point 6) : l'analyse est faite par lecture de code (isolation transactionnelle Prisma/Postgres par défaut = READ COMMITTED) ; aucun test de charge/concurrence réel n'a été exécuté pour confirmer si la fenêtre est réellement atteignable en pratique, ni si le front fournit systématiquement un `idempotencyKey` (le front n'a pas été audité pour ce point précis).
- **Devinabilité réelle des clés S3** (B4d) : les clés sont des UUID v4 (`buildKey()` dans `upload.service.ts:118-121`), donc non énumérables par force brute en pratique — le risque décrit suppose une fuite de la clé par un autre canal (ex. B4c), pas une découverte directe. Sévérité assignée en conséquence (moyenne, pas élevée).
- **SPF/DKIM/DMARC du domaine d'envoi** (B6b) : hors du code source, non vérifiable depuis ce dépôt.
- **C1-C7 et items de conformité produit** : explicitement non traités (cf. Rapport 1), faute de définition récupérable.
- Le comportement exact du front (`client/src`) sur la consommation de `doc.url` a été vérifié seulement pour `DocumentsPage.tsx` — d'autres écrans (ex. téléchargement de facture, de proposition) n'ont pas tous été grep exhaustivement pour le même anti-pattern ; il est possible que le problème B4c soit plus répandu que ce seul fichier.

---

## Méthodologie

### Fichiers lus intégralement dans cette session
- `server/src/services/invoice.service.ts` (447 lignes)
- `server/src/utils/encryption.ts` (43 lignes)
- `server/src/services/approval.service.ts` (203 lignes)
- `server/src/services/creditNote.service.ts` (168 lignes)
- `server/src/services/commission.service.ts` (127 lignes)
- `server/src/controllers/freelancer.controller.ts` (103 lignes)
- `server/src/cache/cacheKeys.ts` (21 lignes)
- `server/src/services/executiveMetrics.service.ts` (27 lignes)
- `server/src/services/healthBoard.service.ts` (8 lignes)
- `server/src/services/dashboard.service.ts` (70 lignes)
- `server/src/controllers/executiveMetrics.controller.ts`, `revenueForecast.controller.ts`, `clientProfitability.controller.ts` (courts, intégraux)
- `server/src/routes/analytics.routes.ts`, `dashboard.routes.ts`, `summary.routes.ts` (intégraux)
- `server/src/controllers/dashboard.controller.ts`, `summary.controller.ts` (intégraux)
- `server/src/services/summary.service.ts` (47 lignes)
- `server/src/services/gscConnection.service.ts` (127 lignes), `searchConsole.service.ts` (137 lignes)
- `server/src/jobs/processors/maintenance.processor.ts` (278 lignes)
- `server/src/utils/projectProgress.ts` (41 lignes)
- `server/src/routes/client.routes.ts`, `project.routes.ts`, `invoice.routes.ts`, `freelancer.routes.ts`, `upload.routes.ts`, `approval.routes.ts` (intégraux ou quasi)
- `server/src/repositories/executiveMetrics.repository.ts` (526 lignes, **relu en entier cette fois** — contrairement à la lecture partielle 150/526 documentée dans `audit/10-audit-metier.md`)
- `server/src/repositories/healthBoard.repository.ts` (129 lignes), `clientProfitability.repository.ts` (132 lignes), `revenueForecast.repository.ts` (141 lignes)
- `server/src/repositories/invoice.repository.ts` (lignes 1-190 sur le total), `client.repository.ts` (lignes 1-120), `project.repository.ts` (lignes 1-175), `task.repository.ts` (lignes 1-65)
- `server/src/services/documentGenerator.service.ts` (541 lignes, intégral)
- `server/src/services/upload.service.ts` (290 lignes, intégral)
- `server/src/services/document.service.ts` (102 lignes, intégral)
- `server/src/controllers/upload.controller.ts` (lignes 70-100)
- `server/src/services/managerPermission.service.ts` (lignes 1-45)
- `server/src/middlewares/rbac.middleware.ts` (47 lignes, intégral)
- `server/src/services/emailTemplates/base.ts` (99 lignes, intégral), `index.ts` (lignes 1-100)
- `server/src/services/auth.service.ts` (grep ciblé + lecture des lignes 120-225 sur reset password / refresh token)
- `CLAUDE.md`, `audit/10-audit-metier.md`, `WORKFLOW_AUDIT_REPORT.md` (intégraux, pour contexte)

### Échantillonnés / vérifiés par grep ciblé plutôt que lecture intégrale
- Recherche exhaustive de `$queryRaw`/`$executeRaw` sur tout `server/src` (4 occurrences, toutes localisées et lues).
- Recherche exhaustive de `hourlyRate` sur tout `server/src` (17 occurrences, toutes contextualisées).
- Recherche exhaustive de `requirePermission(` sur tout `server/src/routes` (comparée module par module contre la liste `MODULES` de `managerPermission.service.ts`).
- Recherche de `/trash`, `/restore`, `/archive` sur tout `server/src/routes`.
- Recherche de `INTEGRATIONS_ENCRYPTION_KEY`, `MAIL_FROM`/`EMAIL_FROM`/sender domain sur tout `server/src`.
- `server/prisma/schema.prisma` : non relu intégralement dans cette session (déjà couvert en détail par `audit/10-audit-metier.md`) — uniquement consulté par grep ciblé pour confirmer la présence de champs `deletedAt`/`archivedAt`.

### Non couvert / hors périmètre de cette session
- Aucune base de données réelle disponible pour exécuter une réconciliation SQL directe (B3 traité par relecture de code uniquement, voir Zones d'ombre).
- Le front `client/src` n'a été inspecté que ponctuellement (`DocumentsPage.tsx` pour B4c) — pas d'audit front systématique.
- C1-C7 et items de conformité produit du round 3 : explicitement exclus, faute de document de référence récupérable.
